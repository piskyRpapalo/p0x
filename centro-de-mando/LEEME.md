# Centro de Mando · v1 (instantánea estática)

Este `index.html` es la **primera versión**: una foto de una medición, sin
servidor detrás. Se conserva porque es historia y porque sigue abriéndose con
doble clic, sin nada corriendo.

**Lo vivo está en `../hexelion-nexo/`**, que es este mismo panel con un
micro-servidor de biblioteca estándar debajo, seis sensores y el rack entero:

```
cd ~/p0x/hexelion-nexo && python3 servidor.py
```

`http://127.0.0.1:8765` · instrucciones completas en
[`../hexelion-nexo/LEEME.md`](../hexelion-nexo/LEEME.md).

No se creó un segundo servidor aquí a propósito: habría duplicado uno que ya
existe, con sus pruebas, y habría pedido el mismo puerto.
