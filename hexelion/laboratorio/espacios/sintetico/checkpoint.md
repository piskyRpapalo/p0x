# checkpoint · espacio SINTETICO

Este espacio NO contiene feedback de personas. Todo lo que hay aqui lo
genero el rack con `bin/inyeccion_dataset.py` para probar el ciclo de
entrenamiento sin depender de testers.

REGLA DURA: nada de aqui se mezcla con `charla-web`, y ningun LoRA
entrenado con esto se promociona sin decir en su ficha que aprendio de
datos sinteticos. Un modelo que no sabe de que aprendio no se puede
auditar.

El primer NO_DATA de esta cadena: estos pares no prueban que el sistema
sirva a nadie. Prueban que la tuberia funciona, que es otra cosa.
